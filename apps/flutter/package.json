{
  "name": "@t4-code/flutter",
  "version": "0.1.24",
  "private": true,
  "scripts": {
    "dev": "flutter run",
    "typecheck": "flutter analyze",
    "test": "flutter test",
    "build:web": "flutter build web",
    "build:macos": "flutter build macos",
    "build:android": "flutter build apk --debug",
    "build:ios": "flutter build ios --simulator"
  }
}
