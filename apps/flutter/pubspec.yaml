name: t4code
description: "T4 Code Flutter client"
publish_to: 'none'
version: 0.1.24+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.3
  flutter_secure_storage: 10.3.1
  shared_preferences: 2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
